import { describe, test, expect } from '@jest/globals';



describe('example.test.ts', () => {
    test('This is a example test in the root tests directory', () => {
        expect(10).toEqual(10);
    })
})